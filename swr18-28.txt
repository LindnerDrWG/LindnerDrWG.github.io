
COMMENT
           STATISTICS WITH REDUCE ch. 1.8-28
                      by W.G. Lindner 2026  $
                      

COMMNT
     /* ---------------- DEFINITION ---------------- mode
       mode(v, i,j,k,m,n) =        --  by G. Weigt 8/2025
         do( n = 0,
           for(i,1,dim(v),
               k = 0,
               for(j,1,dim(v), test(v[i] == v[j], k = k + 1)),
               test(or(k > n, and(k == n, v[i] < m)), 
                    do(n = k, m = v[i]))),  m )

     The above pseudocode (aka runnable EIGENMATH code)
     should be easily ported to REDUCE. 
     ------------------------------------------------- */

%% NOW it is your turn :)


procedure yourMODE(v);
begin 
  1;  % your task

end;

% ------------------- Example usage ---------------------


X := {1,3,6,6,6,6,7,7,12,12,17};
yourmode(X);
  
