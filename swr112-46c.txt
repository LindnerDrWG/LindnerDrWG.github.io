
COMMENT
           STATISTICS WITH REDUCE ch. 1.12-46c
                      Dr. W.G. Lindner
                      Leichlingen, Germany 2026
;                     
                      
procedure moment(X,r);
begin
  scalar n, mx, sx;
  n   := length(X);  
  mx := (for each el in X sum el)/n; % mean
  sx :=  for each el in X sum (el - mx)^r;
  return sx/n;
end;

procedure kurtosis(X);
          moment(X,4)/moment(X,2)^2;   %--- compatibel with MatLab
    
% ------------------- Example usage ---------------------

on rounded $
print_precision 5 $

Y := {2,3,4,4,4,5,6,7,8,9,10};
kurtosis(Y);         % = 1.19


