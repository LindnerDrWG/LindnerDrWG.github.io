
COMMENT
           STATISTICS WITH REDUCE ch. 2.4-88
                      by W.G. Lindner 2026  $
                      

procedure hgPDF(NN,RR,n,r); 
          binomial(RR,r)*binomial(NN-RR, n-r)/binomial(NN,n)$

procedure hgCDF(NN,RR,n, x);
          for k:=0:floor(x) sum hgPDF(NN,RR,n,k) $ % 

% -------------------- Example usage ---------------------

on rounded$
print_precision 5$


% SOLUTION:
% --- We have F(X>=3) = 1 - F(X<=2), so

1 - hgCDF(49,6,6, 2);

% --- Table to draw 0..6 numbers right out of 49

for i:=0:6 do write { i, hgCDF(49,6,6,i) };

