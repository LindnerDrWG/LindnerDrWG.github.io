COMMENT
           STATISTICS WITH REDUCE ch. 3.7-180
                      Dr. W.G. Lindner
                      Leichlingen, Germany 2026 $            


procedure weibullPDF1(x,k,b);
          if  x<0 then 0.0 else 
          b*k*x^(k-1)*exp(-b*x^k) $

procedure weibullCDF1(x,k,b);  
          if x>0 then 1 - exp(-b*x^k)$

procedure weibullINV1(p,a,b);
          (-1/b * log(1-p))^(1/k)$

% ------------------- Example usage ---------------------

on rounded $
print_precision 5$

%-- SOLUTION: cf. [6], p.66 ad b:

1 - weibullCDF1(1,0.01,2);   % = 0.13534
